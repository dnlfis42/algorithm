#include <bits/stdc++.h>

using namespace std;

int solution()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	string str;
	int i;
	cin >> str >> i;
	cout << str[i - 1];

	return 0;
}

int main()
{
	return solution();
}